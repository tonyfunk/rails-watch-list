<div class="container">
<h1>Lists</h1>
<% @lists.each do |list| %>
<p><%= link_to list["name"], list_path(list["id"]), class: "text-decoration-none" %></p>
<% end %>
<%= link_to "add a new list", new_list_path, class: "btn btn-primary" %>
</div>
